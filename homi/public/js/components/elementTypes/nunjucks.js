// Necesitarás instalar esta dependencia
const Nunjucks={
    create: (config) => {
      const element = document.createElement('div');
     
      return element;
    },
    prototypeStyle: {
      border: '1px dashed #FF9800',
      backgroundColor: 'rgba(255, 152, 0, 0.1)'
    }
  }

  export default Nunjucks