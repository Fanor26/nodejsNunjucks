export function createOverlay() {
  const overlay = document.createElement('div');
  overlay.classList.add('modal-overlay');

  // Deja que el estilo se aplique en la función 'applyModalStyles'

  return overlay;
}
