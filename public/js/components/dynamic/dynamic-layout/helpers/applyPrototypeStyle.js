export function applyPrototypeStyle(node, label) {
    node.style.border = '1px dashed #ADFF2F';
    node.style.backgroundColor = 'transparent';
    node.style.boxShadow = 'none';
    node.title = label;
  }
  